import React from 'react'
import { Input } from 'semantic-ui-react'

const InputMessage = () => {
    return(
    <Input fluid action='Отправить' placeholder='Введите сообщение...' />
    )};

export default InputMessage;